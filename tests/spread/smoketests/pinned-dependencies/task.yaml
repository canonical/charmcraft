summary: pack a charm with a specific (old) dependency version

include:
  - tests/

prepare: |
  tests.pkgs install unzip
restore: |
  charmcraft clean

execute: |
  charmcraft pack
  test -f charm*.charm
  unzip -p charm_amd64.charm venv/distro/distro.py | MATCH '__version__ = "1.7.0"'
