[project]
name = "testcharm"
version = "0.1.0"
description = "a revolutionary charm"
requires-python = "~=3.14.6"  # Python 3.10 specifically for a smaller lock file
dependencies = ["overrides", "ops"]
