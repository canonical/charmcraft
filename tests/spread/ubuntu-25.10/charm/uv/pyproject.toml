[project]
name = "testcharm"
version = "0.1.0"
description = "a revolutionary charm"
requires-python = "~=3.13.0"  # For a smaller lock file and to ensure the base.
dependencies = ["overrides", "ops"]
