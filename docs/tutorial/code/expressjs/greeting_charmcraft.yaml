# configuration snippet for ExpressJS app

config:
  options:
    greeting:
      description: |
        The greeting to be returned by the Express application.
      default: "Hello, world!"
      type: string
