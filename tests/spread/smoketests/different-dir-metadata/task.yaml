summary: pack a charm with separate metadata files from a different directory

execute: |
  # If we pull successfully it means we got the correct files!
  charmcraft pull --verbose --project-dir=charm
